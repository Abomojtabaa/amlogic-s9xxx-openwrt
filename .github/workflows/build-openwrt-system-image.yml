name: Build OpenWrt system image

on:
  workflow_dispatch:

env:
  CONFIG_FILE: config/immortalwrt_master/config
  DIY_P2_SH: config/immortalwrt_master/diy-part2.sh
  TZ: Etc/UTC

jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Initialize Environment
        run: |
          sudo -E apt-get -y update
          sudo -E apt-get -y install $(curl -fsSL https://ophub.org/ubuntu2404-make-openwrt-depends)
          sudo mkdir -p /builder
          sudo chown -R runner:runner /builder

      - name: Clone Source
        working-directory: /builder
        run: |
          git clone -q --single-branch --depth=1 --branch=master https://github.com/immortalwrt/immortalwrt openwrt

      - name: Update Feeds
        run: |
          cd /builder/openwrt
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: Load Config
        run: |
          cp -f ${{ github.workspace }}/${CONFIG_FILE} /builder/openwrt/.config
          chmod +x ${{ github.workspace }}/${DIY_P2_SH}
          cd /builder/openwrt
          ${{ github.workspace }}/${DIY_P2_SH} "192.168.1.1" "true"

      - name: Compile
        id: compile
        run: |
          cd /builder/openwrt
          make defconfig
          make -j$(nproc) || make -j1 V=s
          echo "status=success" >> ${GITHUB_OUTPUT}

      - name: Package for Beelink (Kernel 5.15)
        if: ${{ steps.compile.outputs.status == 'success' }}
        uses: ophub/amlogic-s9xxx-openwrt@main
        with:
          openwrt_path: /builder/openwrt/bin/targets/*/*/*rootfs.tar.gz
          openwrt_board: s905x2
          openwrt_kernel: 5.15.y
          auto_kernel: false
